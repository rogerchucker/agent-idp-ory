[pytest]
testpaths = tests
markers =
  unit: fast isolated tests for helpers and pure logic
  integration: route-level tests with mocked Hydra API behavior
  e2e: full login-consent-admin flow tests through HTTP endpoints
addopts = --strict-markers --cov=main --cov-report=term-missing
